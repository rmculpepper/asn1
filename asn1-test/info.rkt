#lang info

;; ========================================
;; pkg info

(define version "1.0")
(define collection "asn1")
(define deps '("base" "rackunit-lib" "asn1-lib"))
(define pkg-authors '(ryanc))

;; ========================================
;; collect info

(define name "asn1")
