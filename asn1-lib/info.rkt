#lang info

;; ========================================
;; pkg info

(define collection "asn1")
(define deps
  '("base"
    "binaryio-lib"))

;; ========================================
;; collect info

(define name "asn1")
