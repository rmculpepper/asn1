#lang info

;; ========================================
;; pkg info

(define version "1.1")
(define collection "asn1")
(define deps '("base" ["binaryio-lib" #:version "1.1"]))
(define pkg-authors '(ryanc))

;; ========================================
;; collect info

(define name "asn1")
